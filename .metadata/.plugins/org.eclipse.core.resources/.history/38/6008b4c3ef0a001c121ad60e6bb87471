package com.anitesh.persistance;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import com.anitesh.bean.InventoryItem;

@Repository
public interface InventoryItemDoa extends JpaRepository<InventoryItem, long>{
	
	@Query("Select id,availableQuantity,inventoryCode from InventoryItem where inventoryCode=:code ")
	public InventoryItem getInventoryItemByCode(String code);


}
